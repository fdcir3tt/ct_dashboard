services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    restart: unless-stopped
    container_name: dashboard
    volumes:
      - ./data:/app/data:Z
    env_file:
      - app/.env
    ports:
      - 8501:8501